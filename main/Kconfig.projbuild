# LVGL 内存分配器说明（历史）:
# 旧版本曾在此补一个 int 型 LV_USE_STDLIB_MALLOC 符号以切换 CLIB 分配器,
# 但 LVGL 9.2 的 Memory Settings choice（LV_USE_BUILTIN_MALLOC /
# LV_USE_CLIB_MALLOC / ...）才是最终生效的开关 —— int 符号会被
# lv_conf_kconfig.h 按 builtin 分支覆盖, 造成 CUR-001 配置冲突。
# 现直接在 sdkconfig 中选择 CONFIG_LV_USE_CLIB_MALLOC=y（见 sdkconfig）,
# 本文件不再定义任何符号。
